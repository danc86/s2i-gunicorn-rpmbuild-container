from setuptools import setup

setup (
    name             = "testapp",
    version          = "0.1",
    description      = "Example application to be deployed.",
    py_modules       = ['testapp'],
)
